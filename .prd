[
  {
    "id": "add-ws-server",
    "subject": "Add WebSocket audio server to main.js",
    "status": "pending",
    "description": "Import WebSocketServer from ws. Add startWsServer() on WS_AUDIO_PORT (env, default 9888). On binary message, parse as Float32Array and call pushAudioFrame. Call startWsServer() in app ready. Add WS_AUDIO_PORT=9888 to .env.example.",
    "blocking": [
      "create-extension"
    ],
    "blockedBy": [],
    "effort": "small",
    "category": "feature",
    "acceptance": [
      "WebSocketServer imported from ws in main.js",
      "startWsServer function exists and called from app ready",
      "On message: Float32Array from buffer, pushAudioFrame called",
      "WS_AUDIO_PORT in .env.example",
      "main.js stays under 200 lines"
    ],
    "edge_cases": [
      "Non-binary messages - skip silently",
      "pushAudioFrame handles null pcmInput gracefully already"
    ]
  },
  {
    "id": "create-extension",
    "subject": "Create Chrome extension for tab audio capture",
    "status": "pending",
    "description": "Create extension/ with manifest.json (MV3), background.js (service worker), offscreen.html, offscreen.js (audio capture + WS send), popup.html, popup.js. offscreen.js: AudioContext 48kHz, getMediaStreamId -> getUserMedia -> createMediaStreamSource -> ScriptProcessorNode 4096 2ch -> interleave L+R -> send raw Float32Array ArrayBuffer over WS.",
    "blocking": [
      "commit-push"
    ],
    "blockedBy": [
      "add-ws-server"
    ],
    "effort": "large",
    "category": "feature",
    "acceptance": [
      "manifest.json valid MV3 with tabCapture, offscreen, storage, activeTab",
      "background.js handles START/STOP from popup, creates offscreen doc, passes streamId",
      "offscreen.js: getMediaStreamId -> getUserMedia -> Web Audio -> ScriptProcessorNode -> WS binary",
      "popup.html shows status, WS URL input, Start/Stop button",
      "popup.js persists WS URL in chrome.storage.local",
      "All files under 200 lines, no comments"
    ],
    "edge_cases": [
      "WS disconnects during capture - reconnect",
      "tabCapture.getMediaStreamId MV3 approach required"
    ]
  },
  {
    "id": "commit-push",
    "subject": "Commit and push all changes",
    "status": "pending",
    "description": "git add -A, git commit -m 'Add browser extension audio source and fix CDP proxy routing', git push origin master, git push entrypoint master",
    "blocking": [],
    "blockedBy": [
      "create-extension"
    ],
    "effort": "small",
    "category": "infra",
    "acceptance": [
      "git status --porcelain is empty",
      "Both remotes updated"
    ],
    "edge_cases": [
      "entrypoint remote may not exist - check first"
    ]
  }
]